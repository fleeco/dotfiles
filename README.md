# .d.o.t.f.i.l.e.s.

These are my based dotfiles for Nixos/Home manager. If you're running nix in a VM and follow the nix install guide to partitioning by label, you should be able to do that part - nix-shell -p git and then clone this repo down and use the config.
